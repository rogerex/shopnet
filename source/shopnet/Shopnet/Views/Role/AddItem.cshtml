@model Shopnet.Models.AssignItemsRoleViewModel 

@{
    ViewBag.Title = "Add Items";
}

<p>
    
</p>
<h2>Add Item</h2>
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm(new {id_role = Model.roleCocurrency.RoleID }))
{
    <fieldset>         
    @foreach (var item in Model.ItemChecklist)
    {
        @Html.CheckBox(item.Key.Name, item.Value)
        @Html.Label(item.Key.Name)
        <br />
    }
        
        <p>
        <input type="submit" value="Save" />
        </p>
</fieldset>
}       

<div>
    <a href="@Url.Content("~/Role/")">Back</a>
</div>